import styled from 'styled-components'

import {
    background,
    margin,
    padding,
    flexbox,
} from '~/components/Theme/helpers'

export const Box = styled('div')`
    ${padding};
    ${background};
    ${margin};
    ${flexbox}
`
