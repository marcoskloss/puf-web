import styled from 'styled-components'

import {
    background,
    margin,
    padding,
    flexbox,
    font,
} from '~/components/Theme/helpers'

export const Box = styled('div')`
    ${padding};
    ${background};
    ${margin};
    ${flexbox};
    ${font};
`
